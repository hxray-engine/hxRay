package;

import sys.io.File;

// this on it's own doesn't do shit lmao
/**
 * Config class. Has a variable for every field in ``config.ini``
 */
@:build(raygen.internal.macros.RgConfigBlder.build())
class RgCfg
{
}